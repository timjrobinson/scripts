DIR="${0:a:h}"

if [[ -f $DIR/aliases ]]; then
    . $DIR/aliases
fi

if [[ -f $DIR/envvars ]]; then
    . $DIR/envvars
fi

PATH="$DIR/:$PATH"

mcd ()
{
    mkdir -p -- "$1" &&
      cd -P -- "$1"
}

setxkbmap -layout us -option caps:escape
