export LPASS_AGENT_TIMEOUT=0

if [ -n "$BASH_VERSION" ]; then
	export HISTSIZE= 
	export HISTFILESIZE=
fi
if [ -n "$ZSH_VERSION" ]; then
	HISTFILE=~/.zsh_history
	HISTSIZE=999999999
	SAVEHIST=$HISTSIZE
fi

export ETH_RPC_URL=http://192.168.50.104:8545
